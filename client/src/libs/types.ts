export interface UserType {
  id: string;
  username: string;
  admin: boolean;
}

export interface MenuType {
  id: string;
  name: string;
  divide: string;
  native: string;
  unit: string;
  price: number;
}
